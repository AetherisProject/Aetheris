[workspace]
members = [
    "core",
    "platforms/desktop",
    "platforms/cli",
    "platforms/web",
]
resolver = "2"

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
strip = true

[profile.release-with-debug]
inherits = "release"
debug = true
strip = false